<UserControl x:Class="ZeroStock.Pages.Controls.DeliveryDocumentGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" xmlns:my="clr-namespace:ZeroStock.Entities" Loaded="UserControl_Loaded">
    <Grid>
        <DataGrid AutoGenerateColumns="False" EnableRowVirtualization="True" HorizontalAlignment="Stretch" 
                  ItemsSource="{Binding}" Name="deliveryDocumentHeadersDataGrid" SelectionChanged="deliveryDocumentHeadersDataGrid_SelectionChanged"
                  RowDetailsVisibilityMode="VisibleWhenSelected" VerticalAlignment="Stretch">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="dateColumn" Binding="{Binding Path=Date}" Header="Date" Width="140" />
                <DataGridTextColumn x:Name="supplierName1Column" Binding="{Binding Path=Supplier.Name1}" Header="Proveedor Nombre1" Width="200" />
                <DataGridTextColumn x:Name="noteColumn" Binding="{Binding Path=Note}" Header="Note" Width="*" MaxWidth="250" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
