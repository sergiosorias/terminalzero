<UserControl x:Class="ZeroConfiguration.Pages.UserLogIn"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid x:Name="layoutRoot">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Name="tbName" Text="Nombre:" Margin="3"/>
        <TextBox Name="nameText" Grid.Column="1" Width="150" Margin="1" Text="{Binding UserName}"/>
        <TextBlock Name="tbPass" Grid.Row="1" Text="Password:" Margin="3"/>
        <PasswordBox Name="namePass" Grid.Row="1" Grid.Column="1" Width="150" Margin="1" PasswordChanged="namePass_PasswordChanged"/>
    </Grid>
</UserControl>
