<UserControl x:Class="ZeroConfiguration.Controls.Users"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <WrapPanel>
            <Button Content="Agregar" Style="{DynamicResource buttonTextStyle}"/>
            <Button Content="Eliminar" Grid.Column="1" Style="{DynamicResource buttonTextStyle}"/>
            <Button Content="Cambiar" Grid.Row="1" Style="{DynamicResource buttonTextStyle}"/>
        </WrapPanel>
        <Grid Grid.Row="1">
            
        </Grid>
    </Grid>
    

</UserControl>
